# Project-wide Gradle settings.
# IDE (e.g. Android Studio) users:
# Gradle settings configured through the IDE *will override*
# any settings specified in this file.
# For more details on how to configure your build environment visit
# http://www.gradle.org/docs/current/userguide/build_environment.html
# Specifies the JVM arguments used for the daemon process.
# The setting is particularly useful for tweaking memory settings.
org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8
# Run independent Gradle work in parallel where possible.
org.gradle.parallel=true
# Enable Gradle daemon for faster builds.
org.gradle.daemon=true
# Enable build caching.
org.gradle.caching=true
# Reuse the configuration phase across compatible builds.
org.gradle.configuration-cache=true
org.gradle.configuration-cache.parallel=true
# AndroidX package structure to make it clearer which packages are bundled with the
# Android operating system, and which are packaged with the app.
android.useAndroidX=true
# Kotlin code style for this project: "official" or "obsolete".
kotlin.code.style=official
# Enable incremental compilation for Kotlin.
kotlin.incremental=true
# Use precise Java tracking for incremental compilation.
kotlin.incremental.usePreciseJavaTracking=true
# Use classpath snapshot for incremental compilation.
kotlin.incremental.useClasspathSnapshot=true
# Enables namespacing of each library's R class so that its R class includes only the
# resources declared in that library itself and none from its dependencies.
android.nonTransitiveRClass=true
